apiVersion: v1
kind: Service
metadata:
  name: my-helloWorld-svc
  labels:
    app: helloWorld
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: helloWorld
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-helloWorld-app
  labels:
    app: helloWorld
spec:
  replicas: 3
  selector:
    matchLabels:
      app: helloWorld
  template:
    metadata:
      labels:
        app: helloWorld
    spec:
      containers:
      - name: helloWorld
        image: codehero92/hello-world-webapp
        ports:
        - containerPort: 8080
