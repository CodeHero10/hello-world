apiVersion: v1
kind: Service
metadata:
  name: {{ include "poc_helloworld.fullname" . }}
  labels:
    {{- include "poc_helloworld.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "poc_helloworld.selectorLabels" . | nindent 4 }}
